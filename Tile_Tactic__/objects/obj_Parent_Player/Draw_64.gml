/// @desc
if !(charge == -1)
{
	for (var i = 0; i < array_length(charge); ++i)
	{
		var 
		switch (charge[i])
		{
			case "fire": draw_sprite_ext(spr_Fireball_GUI, 0,  100 + (i * 20), 100, 1, 1, 0, c_white, 1);
				break;
		}
		
	}
}